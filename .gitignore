# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# ---- Secrets / local config ----
# Never commit credentials, tokens, or environment files
.env
.env.*
*.pem
*.key

# ---- Azure ----
# Azure CLI cache / local auth artifacts
.azure/

# ---- Bicep ----
# Compiled ARM output from `bicep build` (source of truth is the .bicep file)
*.json.tmp
main.json

# ---- Lab outputs (regenerable, may look sensitive) ----
# Generated mock PII — the generator script is the artifact, not its output
mock_pii.csv
scripts/mock_pii.csv
# Audit findings — contains live exposed-blob URLs; regenerate by running the auditor
incidents/audit-report.json

# ---- OS / editor cruft ----
.DS_Store
Thumbs.db
.vscode/
*.swp
